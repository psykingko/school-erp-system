This directory is intended to store submitted assignment files uploaded by students.

In a frontend-only mock environment, files might not be physically saved here during runtime (unless there's a backend server handling the upload). However, this folder structure represents where these files should be stored.
